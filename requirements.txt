# MEDI-IOT EyeCare — 의존성
# shared-libraries는 볼륨 마운트로 주입 (pip 설치 불필요)

# ── Web Framework ──────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12   # 파일 업로드 (안저 이미지)

# ── Database ───────────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0            # 비동기 PostgreSQL 드라이버 (런타임)
psycopg2-binary==2.9.12    # 동기 PostgreSQL 드라이버 (Alembic 마이그레이션용)
alembic==1.14.0            # DB 마이그레이션

# ── Validation & Settings ──────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1

# ── HTTP Client ────────────────────────────────────────────
httpx==0.28.1

# ── Stripe (D R3 D1) · S3 (D R3 D2) ───────────────────────
stripe>=5.0.0,<12
boto3>=1.35.0,<2

# ── Redis (세션/캐시) ──────────────────────────────────────
redis==5.2.1

# ── Security ───────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # 비밀번호 해싱
cryptography==44.0.0               # 환자 데이터 암호화

# ── Utilities ──────────────────────────────────────────────
python-dateutil==2.9.0
pytz==2024.2

# ── Vector DB (Week 3) ─────────────────────────────────────
pgvector==0.4.2            # pgvector Python 클라이언트 (SQLAlchemy 연동)

# ── Imaging (Phase 2 W7 preprocessing) ───────────────────
pillow>=10.4,<12

# ── Testing ────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.3
pytest-timeout==2.3.1

[pytest]
asyncio_mode = auto
addopts = -v --tb=short
log_cli = false

[coverage:run]
omit =
    tests/*
    alembic/*
    docs/*
    */site-packages/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    if __name__ == .__main__.:

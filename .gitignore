__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
*.log
alembic/versions/*.pyc

# 런타임 업로드·로컬 테스트 이미지 (저장소에 포함하지 않음)
uploads/
fundus_*_sklee.jpg

# ML 모델 파일 (git에 올리지 않음 — S3/MinIO 또는 로컬 저장)
models/*.onnx
models/*.onnx.data
models/*.pt
models/*.pth
models/*.bin
models/incoming/
data/
datasets/
*.csv
wandb/
reports/
# Training kit runtime (large artifacts)
training/data/
training/reports/
training/models/
training/__pycache__/
training/.pytest_cache/
# Manifest (대용량 — GPU 서버에서 생성)
training/manifests/unified_*.json
training/manifests/*_eyepacs.json
!training/manifests/sample_*.json
!training/manifests/README.md
# CI workflow 는 추적 (.github/workflows/ci.yml) — Appendix F cicd-guide.md
